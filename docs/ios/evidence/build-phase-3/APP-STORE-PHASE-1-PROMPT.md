# Owner handoff: App Store Phase 1

This is a proposed future prompt and Git promotion sequence. Build Phase 3 did not push, open a pull request, dispatch hosted CI, merge, configure signing, create/request secrets, contact either store console, or authorize those later actions. The final Build Phase 3 report and accepted evidence manifest identify the exact source to promote; an unreviewed later branch tip is not implicitly approved. The tested product checkpoint is `e6c0c65c9c5ad84e2f961839c5eede753c25448e`; the final report/evidence commit is its descendant and is supplied in the completion response. Verify the exact source fingerprints in the accepted manifest and inspect any intervening product change before promotion.

## Proposed owner-controlled Git promotion

After accepting the consolidated Phase 3 report and its evidence, the owner can authorize this sequence as one reviewable integration operation:

1. Verify `/Users/bavabangaru/Developer/pic-pac-poe-ios-native-parity` is clean on `codex/ios-native-parity`, and its commit matches the exact accepted final delivery commit, with its documented README-only native delta from the tested `e6c0c65` product. Confirm the canonical checkout remains clean at `dc7cb397ad335783c13c7976d4cef816d2fc0909`. Inspect the configured remote, target branch and current remote state read-only. Preserve the existing branch/worktree and all history.
2. Push the exact implementation branch to its verified repository without force. The added branch push trigger runs the unsigned verification workflows.
3. Open one pull request into the owner-selected integration branch, linking the consolidated report, post-fix visual contact sheet, motion/sequence evidence, fixture governance, native resource provenance and accurately remaining device/runtime gates. Do not invent a base branch from historical documentation.
4. Let GitHub-hosted CI actually execute. Require Android fixtures/regressions, Android UI flows, native package tests, hosted app integration, XCUITest flows, governed snapshots and unsigned Debug/Release builds to pass. Inspect stored artifacts. Environment failures are failures to investigate; preserve the approved snapshot toolchain and never automatically replace baselines.
5. Review the complete diff and accepted visual result. Confirm no ordinary product defect remains and no testing hooks enter Release. Physical-device and iOS 17 runtime validation remain visible release gates; a simulator pass does not close them.
6. Merge only after the owner approves the concrete pull request and passing hosted results, using a history-preserving merge. Record the resulting merged SHA and its relationship to the accepted implementation commit. Do not create a release tag, rewrite/rebase/reset the implementation branch, or move the protected canonical checkout as a side effect.

No step above was executed during Build Phase 3. A future task must receive owner authorization for push, PR creation and merge before performing them. The deliberately null Android `releaseCommit` and `releaseTag` remain untouched; the iOS promotion does not resolve the separate Android release identity.

## Copyable App Store Phase 1 prompt

> Resume Pic-Pac-Poe from the completed Build Phase 3 implementation in `/Users/bavabangaru/Developer/pic-pac-poe-ios-native-parity`, branch `codex/ios-native-parity`. Keep the canonical checkout `/Users/bavabangaru/Developer/pic-pac-poe` untouched unless I explicitly authorize a later synchronization. Do not recreate, reset, rebase, delete or replace the branch or worktree.
>
> Read `docs/ios/ARCHITECTURE.md`, `docs/ios/VERIFICATION.md`, the complete required-reading map in `docs/ios-handoff/PIC_PAC_POE_IOS_HANDOFF.md`, `docs/ios/evidence/build-phase-3/REVIEW.md` and `docs/ios/evidence/build-phase-3/manifest.json`, the approved `baselines/REVIEW.json`, snapshot comparison, final UI and lifecycle/performance reports, `docs/ios/evidence/build-phase-3/CI-CONTRACT-AUDIT.md`, native asset provenance, and all recorded remaining gates before taking action. Verify the implementation is clean and matches the accepted source. If an owner-approved PR was already merged, establish the exact implementation-to-merge relationship instead of assuming HEAD or inventing a tag.
>
> Execute **App Store Phase 1 — Signing and TestFlight Readiness** as one coherent owner-supervised phase. Preserve the complete Form Playground 2.0 product, exact shared contracts, offline data behavior, bundle coordinate `dev.saipranith.picpacpoe`, version 2.0.0/build 1 unless verified Apple distribution history requires a reviewed increment, iOS/iPadOS 17 minimum, native controls and all accepted visual/motion/accessibility behavior. Do not turn release preparation into another product-build phase. Fix any newly discovered ordinary defect within the existing architecture and rerun the affected gates.
>
> First distinguish what has already been executed from what remains unavailable. Local iOS 26.5 simulator evidence is not iOS 17 runtime evidence, physical-device proof or a hosted CI result. If Git promotion has not been explicitly authorized and completed, present the proposed owner-controlled sequence above for review; do not push, open a PR or merge based on the earlier unsigned-build authorization.
>
> Perform read-only readiness checks and prepare a concrete signing/archive plan before requiring owner participation. Establish the owner's Apple Developer membership/team authority, agreements status, identifier ownership and intended TestFlight destination using existing authorized access. Registration, signing configuration, provisioning, credential creation, private-key access and external mutations require the relevant owner authorization; the earlier build phases provide none. Never ask me to paste a certificate password, private key, API secret or recovery code into chat, commit credentials, or print secret values. Prefer existing protected platform signing facilities and direct owner handling of account/security prompts.
>
> Once I authorize the concrete signing plan, configure only the required app signing, verify the final archive's identity/resources/capabilities and validate a Release archive. Keep archive, export and upload as separately evidenced actions; prepare the exact artifact and proposed destination for my approval before any TestFlight upload. Do not interact with Google Play or modify Android signing/release identity. Do not submit to App Review or release publicly in this phase.
>
> Complete the remaining release validation on available approved physical iPhone/iPad hardware and on an already available compatible iOS 17 environment. Do not install runtimes or change the global developer directory without my authorization. Record each actual device/OS, source revision, signed artifact identity, tester and result. Cover complete Classic, Local, Easy/Medium/Hard and AI Lab play, rematches, interruption/relaunch/restoration at each presentation boundary, terminal settlement, largest intended Dynamic Type/display zoom, both themes, Reduce Motion, VoiceOver reading/focus/announcements and modal isolation, Switch Control/external keyboard navigation including focus recovery and a successful first move after rematch, real haptic and audio behavior, cold/warm Hard and MCTS responsiveness, cancellation, device frame pacing, and fresh install/update behavior. First-release iOS update validation must use a documented prior approved test build; do not claim an existing public iOS version.
>
> Keep unavailable checks explicitly open instead of relabeling simulator evidence. Review final dependencies and API use for the later privacy-manifest/declaration work, but do not silently invent legal/store answers or extend this phase into metadata submission. Public legal URLs, App Privacy/export/age declarations, store art/metadata and final submission belong to App Store Phase 2 unless I explicitly broaden the scope.
>
> At completion provide one consolidated App Store Phase 1 report containing exact source/archive/export/upload identities and hashes, all actual local/hosted/device results, any remaining external gates, TestFlight processing/install evidence only if upload was explicitly approved and executed, and one App Store Phase 2 prompt. Leave the authorized implementation checkout clean. Stop only for a genuine missing authority, credentials handled by me, destructive operation, material product decision, or phase completion; complete all safe preparatory work before asking for a final approval.
