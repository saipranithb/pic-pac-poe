# Final Build Phase 3 player-driven UI verification

PASS: 26 of 26 source-discovered XCUITests passed, with 0 failed, skipped, or expected failures. The inventory gate passed. Test execution took 1036.414 seconds; the xcresult summary start-to-finish interval was 1063.142 seconds. The original successful 24-test package remains historical evidence; this run adds and executes the two large-text layout regressions after the final product fixes.

Source was clean `e6c0c65c9c5ad84e2f961839c5eede753c25448e` on `codex/ios-native-parity` throughout this run. `stableSource` is true. Build inputs SHA-256: `2be2a20569eb23b3b22553c8e39ed8ce25ebeeb23fbab3665a8a3d175eb2dec3`. Broad iOS source SHA-256: `7294c6d0018d9c3b39bfb1c5c56fec2ea72a1554a02a6e3a3dc95e93d041e482`. Exact toolchain: Xcode 26.6, build 17F113. The iPhone 17 Pro simulator `F36B6568-2C34-4D22-A88D-0AB8C240E6CA` ran iOS 26.5 build 23F77 on arm64. Its task-owned simulator was shut down successfully after export; data was preserved. Setup and shutdown records are included.

The suite completes 15 ordinary games using production AI workers, randomness and timing: first game and rematch for Classic, Pic-Pac Local, Easy, Medium, Hard, MCTS and Q-learning, plus a Classic draw. Additional tests cover terminal AI settlement, selected and committed targets across relaunch, human start/reveal restoration, settings persistence, locked/modal privacy semantics, row-major board geometry, largest text and landscape reachability, and native accessibility audits.

The two added regressions verify all nine board-cell frames within 0.5pt across human start/placement and computer thinking/target/placement/settled stages at regular and AX3 text, with top and explicitly bottom-anchored fixture viewports. They also require all four complete combined tutorial step labels to remain reachable at AX3 and AX5 using actual swipes. The tutorial numbers and punctuation received separate visual inspection because a combined accessibility label does not prove painted punctuation layout.

All 98 original native PNG exports and their timestamps/hashes are retained. All 37 required temporal/stage families are present. No video was exported in this successful run; the governed timestamped native-frame sequences provide temporal evidence. `manifest.json`, `test-summary.json`, `test-tests.json`, `inventory-gate.json`, `test.log`, and the attachment inventory preserve the executable result and source/device provenance.

`sequence-contact-sheet.png` shows 17 complete portrait captures without cropping: seven human/computer frames, six win/draw settlement-interruption-result frames, and four target restoration/commit frames. Labels show the actual monotonic capture uptime and explicit x10 or x20 essential-stage holds. These sequences prove presentation order and restoration, not ordinary-speed frame pacing. The seven-frame player sequence uses real input and production AI with x10 holds; interrupted stage fixtures start from explicit valid snapshots and use production transitions with x20 holds. Thinking, when needed, is separately covered by held-stage and hosted pending-search tests. `sequence-sheet-provenance.json` preserves each capture timestamp/hash and renderer provenance. Normal-motion Home and scene-inactivity sequences are retained in the separate Home/manual motion package.

Full-resolution review in this package covers 33 original images: all eight new tutorial originals, all 17 sheet sequence originals, six representative AX3 geometry originals, and two AI Lab artifact-context originals. Every listed original was opened at its full 1206x2622 resolution before sheet review. All inspected product results passed. The corrected tutorial punctuation remains on the first line, target-to-placement board geometry stays fixed at top and bottom anchors, actor and symbol identities are distinct, draw settlement reads “1 piece remains”, and results appear after settlement. `visual-review.json` records the exact per-image scope and hashes. This is not a claim that all 98 UI exports were visually reviewed; the full governed baseline matrix has its own complete original-image review inventory.

The suite audited 28 viewports for contrast, hit region, sufficient element description, text clipping and traits. There was **one acknowledged native auditor artifact and zero unhandled findings**, not zero native findings: the exact dark AI Lab introductory paragraph at the bottom 402x874pt viewport. Its accessibility bounds were `{{20.0, -1.7}, {340.3, 64.3}}`, with only 0.6666666666665719pt intersecting the visible content below the 62pt top edge. Its visible top audit passed first; recorded contrast was 9.856:1. The finding text and screenshot remain intact in `semantics/FD618FF6-1618-45BD-B319-67654995116D.txt` and the native frame exports. All other native findings fail the test.

The raw xcresult remains at `/private/tmp/picpac-phase3-ui-layout-final-e6c0c65-20260925/phase3-ui-e6c0c65c9c5a.xcresult`: 273 files, 48,134,051 bytes. `raw-xcresult-inventory.json` records every raw file's relative path, byte count and SHA-256. A byte-identical self-contained copy is also included as `raw-result.xcresult`; all 273 copied files were verified against original sizes and SHA-256 values. The original bundle was preserved. Portable result JSON, native attachments and logs are also included here. Nonfatal debugger-version diagnostics in the log did not prevent automation or produce a test failure.

This is local simulator verification. It does not claim a GitHub-hosted Actions run, an iOS 17 runtime run, physical VoiceOver/speech behavior, Switch Control hardware behavior, keyboard ergonomics, physical sound/haptics, or physical-device frame pacing. The earlier external CUA bridge failure prevented the supplemental Release keyboard follow-up; this suite does not relabel that unexecuted interaction as a success. No push, signing, upload, submission or store action was performed for this run.
