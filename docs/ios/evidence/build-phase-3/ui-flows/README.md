# Final player-driven UI verification

Accepted from clean checkpoint `8eabb669b6b154e982d82d3e8de8b34b778e337b`, build-input SHA-256 `fa7edfd6ad37fed863f0b6231da42ff248cf709c7e084e82d1f3d36ec573cfcd`. The governed runner confirmed unchanged source, **24 passed / 0 failed / 0 skipped / 0 expected failures**, and all 24 source-discovered XCTest methods. The xcresult summary elapsed interval was 894.319 seconds. Actual device: iPhone 17 Pro, iOS 26.5 (23F77), arm64; Xcode 26.6 (17F113). The original xcresult is retained at the path in `manifest.json`; portable summaries, test tree, attachments, command and full log are retained here.

The run includes 15 complete games at ordinary timing using real player control, production randomness and production AI: Classic, Pic-Pac Local, Easy, Medium, Hard, MCTS and Q-learning each finish an initial game and rematch; Classic also finishes a draw. Separate checks cover persisted settings, navigation, relaunch, scene interruption, selected targets and already committed moves, terminal win/draw settlement, stale AI isolation, row-major board order, fixed square hitboxes, duplicate/occupied activation, privacy, landscape, maximum Dynamic Type and reachability.

All **66 original timestamped screenshots** are retained, including all **37 required temporal/stage families**. [Controlled sequence contact sheet](sequence-contact-sheet.png) presents 17 complete portrait frames without cropping: seven human/computer sequence stages with essential holds multiplied by 10, six terminal win/draw interruption frames and four selected-target restoration frames with holds multiplied by 20. Labels retain stage, multiplier and monotonic uptime. These are order/restoration observations, not ordinary-speed pacing measurements. The separate interactive motion package preserves ordinary-speed actual recordings. Original landscape captures retain their native EXIF orientation; no screenshot bytes were transformed.

Native accessibility audits checked **28 viewports** across both themes. There were **zero unhandled findings and one acknowledged finding**, the precisely governed dark AI Lab offscreen introductory paragraph contrast artifact. Its original native finding and geometry are preserved under `semantics/` and in the manifest. This is not a zero-finding claim; the fully visible paragraph passed in the same build and the independently measured original-pixel contrast is 9.856:1. Every other contrast, target-region, description, clipping or trait finding would fail the suite.

Independent post-run inspection opened the original terminal draw before-interruption, restored-settlement and result frames at full resolution. Both settlement frames visibly retain the committed final X and the corrected “1 piece remains in the shared bag” footer before the later result overlay. No clipping, premature result or actor/symbol mismatch was found in those frames. The controlled sequence sheet was visually inspected after generation.

This is simulator evidence, not iOS 17 runtime, physical VoiceOver/Switch Control hardware, keyboard ergonomics, audio, haptics or physical frame-pacing certification. GitHub-hosted Actions were not executed. The task-owned UI simulator was shut down successfully after the accepted run; its data was preserved.
