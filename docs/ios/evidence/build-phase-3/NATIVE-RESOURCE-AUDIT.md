# Native resource and minimum-platform audit

The independent source audit found a missing native app icon: Build Phase 2 contained no asset catalog, app-icon build setting or compiled icon metadata. A successful unsigned simulator compile did not establish archive resource completeness. Phase 3 corrects this ordinary product defect without signing or store work.

`ios/PicPacPoe/Assets.xcassets/AppIcon.appiconset/AppIcon.png` is an opaque 1024×1024 sRGB image rendered directly from the unchanged canonical Android `ic_launcher_background.xml` and `ic_launcher_foreground.xml` paths. It preserves the 108×108 coordinate system, cocoa diagonal band and grid, coral X, pistachio O, original symbol proportions and overlap. It does not copy an Android adaptive mask or screenshot; iOS applies its native icon shape. The full-resolution PNG was visually inspected, and two renderer executions produced byte-identical output. No new branding or external artwork was introduced.

The reproducible renderer is `ios/Scripts/GenerateNativeAppIcon.swift`. It supports only the commands in the current canonical vectors and fails on an unsupported command instead of silently approximating source changes. Exact source, output, renderer and license hashes are in `native-asset-provenance.json`. The source artwork and adaptation use the repository's Apache-2.0 license, Copyright 2026 Pic-Pac-Poe contributors; the original project `LICENSE` remains authoritative. The existing Fredoka files and SIL OFL license are unchanged.

A semantic `LaunchBackground` named color now supplies cream `#FBF1DE` for light system appearance and cocoa `#251F1C` for dark system appearance through `UILaunchScreen.UIColorName`. This avoids the platform's unstyled launch background while restoration loads. Native launch screens follow system appearance; a separately saved in-app theme is applied after local settings restore. The launch screen is a static background, does not reconstruct a match, and adds no decorative or game state.

The catalog is included in the ordinary application Resources phase and `ASSETCATALOG_COMPILER_APPICON_NAME` selects `AppIcon`. The Release verifier now checks compiled icon metadata for iPhone and iPad, the catalog, launch color reference, exact bundled Fredoka/OFL and Q-policy bytes, absent test fixtures/signing material, bundle/version identity, iOS 17 minimum, and exclusion of development launch hooks and LLVM coverage/profile instrumentation. The shared CI-contract gate checks resource provenance hashes without refreshing them.

Source inspection retains iOS 17 compatibility: Observation, the two-argument/initial `onChange` form and SwiftUI accessibility APIs used by the app are available at the declared minimum or earlier. A successful build with deployment target 17.0 confirms compile-time API availability only; it cannot prove runtime behavior on iOS 17. The local host has only iOS Simulator 26.5, as recorded by `ci-logs/installed-runtimes.json`. No runtime installation or global developer-directory change occurred.

Historical binary evidence is retained with its original source. The earlier builds on `ad1a3b40570406a7cf967c600a7c88516dbf2c0c` passed the then-current identity/resource verification, but independent inspection found LLVM coverage instrumentation. Those binaries are superseded; the [coverage finding](defects/release-coverage/finding.json) records that defective source, its exact binary hashes and compiler flags. The corrected pre-layout four-build matrix on `03d44f794ef98e66ebeb63cd467313ece2331b91` passed the strengthened gate; its [manifest](unsigned-builds-final-20260925/manifest.json) remains valid historical evidence, not final post-layout acceptance.

The final four-build matrix passed on clean `e6c0c65c9c5ad84e2f961839c5eede753c25448e` with build-input SHA-256 `2be2a20569eb23b3b22553c8e39ed8ce25ebeeb23fbab3665a8a3d175eb2dec3`. [The final manifest](unsigned-builds-layout-final-20260925/manifest.json) retains exact commands, toolchain, source and full artifact hashes; all 17 build-evidence checksum entries pass. Every artifact is unsigned with minimum iOS 17.0. Both actual Release executables contain no development launch hooks or LLVM coverage/profile sections, and their compiler commands contain no instrumentation flags. Every simulator Mach-O slice (arm64 and x86_64) and device SDK slice (arm64) declares its correct platform and minimum 17.0. The native icon metadata for iPhone/iPad, exact canonical resources and launch dictionary containing only `UIColorName: LaunchBackground` pass the actual-bundle gate. The only final build warning is the expected App Intents metadata extraction skip because the app does not depend on AppIntents.framework.

The accepted local package originated at `/tmp/picpac-phase3-unsigned-builds-layout-final-unsandboxed-20260925`, with artifacts under `/tmp/picpac-phase3-layout-final-build-e6c0c65c9c5a-20260925T105458Z/`. Its `failed-sandbox-attempt` subdirectory preserves the initial attempt that failed before compilation when sandbox restrictions blocked required SwiftPM/Clang caches and CoreSimulator services. The unchanged authorized unsigned commands then passed with sandbox escalation in fresh paths. This environment failure is not counted as an accepted build or a product defect.

[The final Release launch frame](release-launch-layout-final-20260925/release-launch-full-frame.png) comes from the exact final simulator Release app, bundle SHA-256 `f7c28bdb49ffb69905731865ad3629a21e6dc4714eee7076225d28cb472bab28`. It was installed over preserved app data and launched with no arguments or test hooks on the existing `PicPacPoe Phase3 Performance` iPhone 17 Pro / iOS 26.5 simulator (`97FAA70C-E2D7-4893-9D76-FE172A792433`). The original 1206×2622 PNG was independently inspected and shows complete light Home, all controls and a naturally captured decorative X/O crossfade. The simulator was confirmed shut down afterward. All 5 [launch-evidence](release-launch-layout-final-20260925/manifest.json) checksum entries pass, including the consumed build-manifest copy and original-resolution review evidence.

This is one post-launch still, not proof of every launch frame, temporal animation, fresh-install/update behavior or physical-device appearance. The build gates establish compile-time iOS 17 compatibility and actual unsigned resource/binary integrity; they do not establish iOS 17 runtime execution. Physical VoiceOver/Switch Control/keyboard focus, real sound/haptic quality, device pacing and signed install/update checks remain explicit device-validation requirements. No iOS 17 runtime or physical-device execution was available. No runtime installation, global developer-directory change, signing, upload or store interaction occurred. Workflow commands were reproduced locally; GitHub-hosted Actions remain **NOT RUN**. Ordinary handoff verification passes, and the sole permitted strict-release failure remains the unchanged null `git.releaseCommit` and `git.releaseTag`.
