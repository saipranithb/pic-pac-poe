# Accessibility, lifecycle and measurement boundaries — final owned-gate results

Final lifecycle/integration/performance run **PASS** at clean source `e6c0c65c9c5ad84e2f961839c5eede753c25448e`: hosted 12/12 with zero skips, package 63/63, optimized audit 1/1. Source hashes were unchanged and the canonical checkout stayed clean at `dc7cb397ad335783c13c7976d4cef816d2fc0909`. The complete UI run also passed 26/26 with zero skips; its actual audit export records one acknowledged offscreen artifact and zero unhandled findings. The consolidated report independently closes snapshots, unsigned builds, Android and handoff gates. Earlier evidence remains tied to its own source.

## Accessibility evidence and limits

The automated contract covers native labels, headings, traits, row-major board order, equal square hitboxes, occupied and locked input rejection, result/reveal/handoff modal isolation, a single final-board summary, one native switch per preference, selected controls, scrolling and largest Dynamic Type. Both-theme Apple accessibility audits cover nine screen families, including five bottom viewports: 28 viewport audits of contrast, hit region, sufficient description, clipping and traits. Latest complete UI execution: **26 passed, 0 failed, 0 skipped; 1,036.414 s test execution / 1,063.142 s result-bundle duration. All 98 original frames and 37 required frame families are present**.

Historical complete UI runs retained one narrow iOS 26.5 finding disposition: the dark AI Lab introduction at the declared bottom anchor is offscreen while its AX rectangle intersects the boundary by at most 1 point and 2% of its height. It is accepted only for that exact label, contrast finding and 402×874-point viewport after the fully visible top paragraph passes in the same build/theme. Original visible pixels measure 9.856:1. The historical native finding, bounds and screenshot remain preserved. The final export independently reproduced this exact artifact; every other finding fails. Final native finding count/dispositions: **1 acknowledged instance of that exact offscreen dark AI Lab artifact, 0 unhandled**. Its boundary intersection was 0.6666666666665719 pt, and the fully visible top audit passed. The actual final export retains its native finding and frame.

Independent final visual review opened all 96 iPad/AX3 originals at original resolution and both profile contact sheets, retaining hashes and 21 directly opened canonical references. The previously rejected tutorial-marker wrapping and moving gameplay geometry are fixed. The short-content bottom fixtures use Debug `.defaultScrollAnchor(.bottom)` and can align content low: these are not normal initial layouts or evidence of a user gesture's final offset. Default-top views and separate player-driven reachability establish the complementary evidence.

The actual interactive Debug recording from `bf384da11190cb45ff21866a6d804c85a35ce333`, using production timing/randomness/AI, demonstrates a complete Classic game through Full Keyboard Access, occupied-cell duplicate activation rejection, result activation and an alternate-starter rematch. It does not record a successful subsequent keyboard move after rematch. A supplemental unsigned Release keyboard attempt never reached interaction: the native automation bridge timed out before returning a Simulator app handle. Those retained 846-second and 282.3-second tool waits are neither app latency nor a passing Release keyboard test. No further identical bridge retry was claimed.

## Lifecycle, announcements and Home motion

The 21 restoration-boundary rows cover Home, Settings, How to Play and AI Lab; Classic play/terminal; Local handoff/reveal/play/terminal; human turn start/reveal/play; computer reveal/thinking/target; nonterminal placing/settling; terminal placing/settling/result. Repeated suspension rejects stale clock, Ready and board actions. Encode/restore/inactive-reconstruction/resume checks prevent repeated draws, moves and feedback; selected targets commit once and committed positions remain unchanged. Cancellation-unaware stale AI completion and slow-persistence search/navigation continuations have explicit regressions. The ordinary defects were fixed, including inactive action acceptance and stale persistence/navigation continuations.

Hosted speech policy checks distinguish target from committed symbol, suppress speech while inactive or on restoration, avoid duplicate direct announcements for focused content and do not repeat placement at settlement. This validates announcement routing and policy, not human VoiceOver speech delivery or focus timing. Real SwiftUI scene/atomic-file restoration and OS background/relaunch behavior are exercised separately by XCUITest and interactive recordings; the hosted UIHostingController active/inactive harness is not itself an OS interruption simulation.

Home uses deterministic elapsed-time X/O selection, with no access to game RNG or persistence. Its repeating state is absent from the restoration snapshot, its accessibility description stays stable and it makes no announcement/sound/haptic calls. Final measurements show zero draw calls, feedback and decorative writes; the one suspension checkpoint is counted separately. Ten original visible-UIWindow frames show normal/Reduce Motion X→O and pixel-identical inactive pairs over at least 1.5 seconds. These window frames exclude the OS status bar and complement the full-screen simulator recordings, which show normal pulse, Reduced Motion and actual background/resume. Latest-source hosted temporal proof **PASS**: all ten final 1206×2622 originals opened at full resolution; exact inactive intervals were 1.625535s and 1.628424s. The 0.0/0.34 frames happen to be identical, so they do not independently prove pulse amplitude; the separate normal-speed recording from `bf384da11190cb45ff21866a6d804c85a35ce333` supplies pulse evidence. `HomeView.swift`, including the motion helpers, is unchanged from that revision through final `e6c0c65`; gameplay layout did change, so the older keyboard/Hard movies remain earlier-source interaction evidence complemented by the final UI/layout sequences. The fresh export contains 12 attachments: ten PNGs and two measurement JSON records.

## Latest-source measurements — accepted local execution

Actual simulator: existing task-owned iPhone 17 Pro (`iPhone18,1`), iOS 26.5 build 23F77, UDID `97FAA70C-E2D7-4893-9D76-FE172A792433`. Xcode 26.6/17F113, Swift 6.3.3, Apple M3 Pro arm64, macOS 26.5.2. No other simulator was booted at measurement start and no concurrent phase compiler/capture ran. Simulator shutdown succeeded before host tests. The shared host was not thermally isolated.

| Gate or measurement | Final actual result |
| --- | --- |
| Hosted integration inventory | 12 passed, zero failed/skipped; 18.344s; exact source inventory |
| Complete Swift package | 63 passed, zero failed/skipped; 34.322s |
| Optimized production-agent audit | 1 passed, zero failed/skipped; 0.526s |
| Simulator cold/warm Hard, three samples each | 76.871500–77.285834ms / 0.020209–0.022833ms |
| Simulator MCTS, exactly 2,000 simulations | 23.446875–24.048583ms |
| Simulator cancellation after request | 0.107125–1.241625ms |
| Home normal CADisplayLink | 186 callbacks; p95/max 16.824/17.189ms |
| Home Reduce Motion CADisplayLink | 185 callbacks; p95/max 16.791/17.133ms |
| Live Hard CADisplayLink | 184 callbacks; p95/max 16.697/22.113ms |
| Optimized host cold/warm Hard, five samples | 75.477959–77.157333ms / 0.016250–0.034833ms; medians 75.767875/0.017500ms |
| Optimized host MCTS/cancellation, five samples | 22.168125–23.234708ms / 0.108958–0.238084ms; medians 22.847500/0.231542ms |
| Host 2ms main-actor heartbeat | 176 samples; p95/max 3.069916/3.089167ms |

The final package is `/tmp/picpac-phase3-post-layout-lifecycle-20260925`, with `REPORT.md`, raw xcresult, exact commands/source fingerprints/app-file hashes/compiler modes, raw measurement arrays, ten inspected originals, contact sheet and 117 checksummed evidence files. All checksum entries verified. `REPORT.md` SHA-256: `b8fabe655fb209bdfc79f439eb31f9b029889269830a789962d4728db5896578`; checksum inventory SHA-256: `2bc45c2b0163765ba329746fca81458f973f7f1afa1c804f1cb3c7e731774c5c`.

Actual callback outliers and all search/cancellation samples were inspected. No callback reached 25ms and no delivered timestamp interval exceeded 1.5× nominal 60Hz. Cold/warm Hard visited 109,706/18 nodes; cancellation was requested 5ms into each fresh search and returned CancellationError. The live Hard stage sequence was `turnStart → revealing → aiTargeting → aiPlacing → aiSettling → turnStart → revealing → playing`. That actual search completed during reveal, so thinking was unnecessary in this run; delayed-search thinking remains independently covered. No broad hang assertion was used as the sole measurement acceptance basis.

The earlier accepted `03d44f7` run and its two isolated 33ms Home callback gaps remain historical evidence. The final log also retains the expected UIKit-host SceneStorage warning and an auxiliary post-test simulator-diagnostic collection error72 (`xcrun` could not locate simctl). All twelve actual tests passed, the outer command returned zero, complete xcresult/attachment export succeeded and per-process developer-directory simulator commands succeeded. This warning is not an omitted test or product-latency result, and global developer selection was not changed to suppress it.

Hosted cadence uses the instrumented Test configuration: app/tests `-Onone` with `TESTING`/`DEBUG`, optimized whole-module Swift package modules, and coverage flags. It is simulator Test evidence, not production Release app pacing. Display timestamps measure opportunities and callback intervals measure main-runloop delivery; neither proves GPU frame completion. Separate optimized host package timing covers algorithms and main-actor progress, not display pacing. Unsigned distribution Release binaries have their own no-testing-hook/no-instrumentation gate. A generic device-SDK build does not execute on hardware.

## Exact remaining external release gates

- **Minimum iOS/iPadOS 17 runtime:** first launch, complete flows, lifecycle/restoration, layout, accessibility and performance on an already available compatible environment. Only iOS 26.5 simulation is available locally; deployment target/API-availability/Mach-O minimum 17 checks establish compile-time compatibility only. No runtime installation or global developer-directory change was performed.
- **Physical VoiceOver:** spoken label and announcement correctness/timing, row-major traversal, heading navigation, focus entry/return across reveal/handoff/result, inactive/resumed speech and absence of privacy leakage. Native AX trees and speech policy cannot certify the actual spoken experience.
- **Physical Switch Control:** scanning order, reachable actions and board cells, selection activation, modal isolation, focus recovery after interruption/results/rematches and actual switch hardware. The reviewed simulator inventory did not expose Switch Control or VoiceOver controls.
- **Physical external keyboard:** complete modes, traversal/focus visibility, occupied/locked action rejection, focus recovery after navigation/background/results and a successful first move after rematch. Recorded simulator Classic/rematch activation is partial ergonomic evidence only; the supplemental Release follow-up remains unexecuted.
- **Physical sound and haptics:** perceptibility and quality of draw/move/result cues, settings behavior, interrupted/resumed behavior and no duplicate feedback under real device conditions. PCM/resource and logical feedback checks do not establish audible/tactile output.
- **Physical performance and launch:** cold/warm Hard, MCTS budget, cancellation, actual device frame pacing under representative thermal/load conditions, launch appearance, install/update and restored frames. Signed install/update must be separately authorized and identified; no physical app was installed or launched here.
- **Hosted CI and promotion:** workflow definitions inspected and commands reproduced locally are distinct from GitHub-hosted execution. Hosted Actions have not run because no push/dispatch was authorized; later runs need actual run IDs and artifacts.

These unavailable checks remain explicit owner-controlled release validation. They do not excuse an ordinary software defect and do not convert simulator evidence into physical or minimum-runtime proof. Signing, TestFlight, metadata/privacy declarations, upload and submission remain outside Build Phase 3. Nothing was pushed, signed, uploaded or submitted by this audit. The owned lifecycle/integration/performance gates are **PASS**; the independently executed complete 26-test UI gate also passed. The consolidated report records the final unsigned release-candidate verdict after evidence assembly and integrity verification; it does not claim a signed/submission-ready app.
