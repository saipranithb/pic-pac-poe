# Interactive simulator audit and normal-speed motion

These are actual full-screen iPhone 17 Pro / iOS 26.5 simulator recordings, not rendered mockups. `source.json` pins clean implementation checkpoint `bf384da11190cb45ff21866a6d804c85a35ce333`, the installed app executable and Debug dylib, and every app-bundle input. The later `ad1a3b4` change only corrects singular bag grammar and adds its assertion; these recordings never show a one-piece count. Final post-fix automated UI frames and governed baselines are separate evidence in the consolidated package.

The app ran with ordinary production timing, production random source and production AI. Its Debug session argument only isolates the real atomic file store. No initial-state fixture, clock multiplier, screenshot freeze or injected move was used. Interaction used visible native Simulator controls, accessibility elements and Full Keyboard Access. Full Keyboard Access was enabled through Settings only in the task-owned disposable simulator; blue focus highlights are native system overlays.

## Evidence

| Recording | What was actually observed |
| --- | --- |
| [keyboard-classic.mov](keyboard-classic.mov) | Complete Classic game by Tab/arrows/Space, X positions 0/1/2 and O positions 7/8, occupied-cell duplicate activation rejected, Player 1 result and keyboard-activated alternate-starter rematch. The final rematch frame retains the native Full Keyboard Access outline at the former Rematch button position. No subsequent keyboard move is recorded. |
| [home-normal.mov](home-normal.mov) | Bag → alternating semantic X/O → board explanation, small bounded normal-motion pulse and switch. The wordmark remains stable after its finite entrance. |
| [home-reduced.mov](home-reduced.mov) | Actual user-selected Reduce Motion, stable illustration geometry with the retained opacity/symbol switch. |
| [home-interruption.mov](home-interruption.mov) | Home, actual system Home/background, app-icon resume, restored Home without replaying the wordmark entrance. A background recording cannot show a hidden app; the hosted inactive UIWindow pixel-invariance test supplies that separate proof. |
| [hard-live-game.mov](hard-live-game.mov) | Complete actual Hard game and rematch. Computer starter inherited from the preceding rematch per canonical rules; O at center, human X at 0, computer X at 8, human O at 1, computer O at 7 completes O column and correctly awards Computer. Rematch retains Hard and makes You the starter. |

[Home motion contact sheet](home-motion-contact-sheet.png), [Hard sequence contact sheet](hard-sequence-contact-sheet.png), [keyboard contact sheet](keyboard-contact-sheet.png), and [interruption contact sheet](home-interruption-contact-sheet.png) contain scaled whole frames without cropping. Each sibling frame directory retains the full 1206×2622 PNGs and requested/actual video timestamps. The MOV files are the complete recordings, including inspection pauses. Variable-frame-rate recording holds unchanged frames; use `actualSeconds`, not a requested extraction time, as the presentation timestamp. The nominal video frame rate is not an app frame-pacing measurement.

The initial Hard sequence visibly includes computer reveal around 21.24 s, thinking at 21.90 s, target at 22.65 s, placing at 23.00 s and settled board at 23.31 s, followed by human reveal. The terminal sequence includes reveal at 206.73 s, target at 207.42 s, placing at 207.80 s, move placed at 208.13 s and result at 208.64 s. These are sampled actual frame timestamps, not exact transition-boundary measurements. The complete recording preserves the intervening frames.

`home-normal.json` and `home-reduced.json` prove that both persisted JSON files retained identical bytes and nanosecond modification times over each observed twelve-second interval. The separate hosted dependency-counter tests cover zero gameplay RNG calls, persistence writes and feedback; speech policy and stable accessibility semantics cover announcements. A simulator recording alone is not proof about inaudible feedback or hidden state.

Native Settings screenshots under `availability/` record the actual Full Keyboard Access switch. The reviewed simulator Accessibility inventory did not expose VoiceOver or Switch Control controls. Native AX labels/traits/order/privacy, automated audits and announcement routing are tested separately; human VoiceOver speech timing, real switch hardware, physical keyboard ergonomics, audio and haptics remain explicit physical-device release checks. Nothing here is physical-device or iOS 17 runtime evidence.

The Hard recording includes human inspection pauses and an automation coordinate-routing error before any move was issued; keyboard interaction then continued successfully. Those pauses are not AI latency. The separate isolated performance report supplies measured search/cancellation/display-link observations.

A further manual keyboard check against the final unsigned Release app was attempted during recovery, but the computer-use bridge timed out before app interaction. [Exact attempt and failure evidence](../recovery-20260925/release-keyboard-bridge/README.md) is preserved. Post-rematch keyboard navigation and placement in that Release build therefore remain unobserved; this is neither a passing follow-up nor evidence that the app rejects or misroutes keys. The earlier Debug recording establishes the complete game, result and rematch activation only. Physical keyboard and Switch Control navigation, including focus recovery after rematch, remain explicit device-validation gates.
